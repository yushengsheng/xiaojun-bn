name: Release Windows EXE

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install build dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller requests eth-account eth-utils

      - name: Prepare entry script
        shell: pwsh
        run: |
          Copy-Item "小军bn.py" "app_entry.py" -Force

      - name: Build EXE (onefile, windowed, no console)
        shell: pwsh
        run: |
          pyinstaller --noconfirm --clean --onefile --windowed --name xiaojun-bn --collect-submodules eth_account --collect-submodules eth_utils app_entry.py

      - name: Package release assets
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path release -Force | Out-Null
          $exeName = "xiaojun-bn-${{ github.ref_name }}-windows-x64.exe"
          $zipName = "xiaojun-bn-${{ github.ref_name }}-windows-x64.zip"
          Copy-Item "dist/xiaojun-bn.exe" "release/$exeName"
          Compress-Archive -Path "release/$exeName" -DestinationPath "release/$zipName" -Force

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v6
        with:
          name: xiaojun-bn-windows
          path: release/*

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = "${{ github.ref_name }}"
          $assets = Get-ChildItem "release/*" | ForEach-Object { $_.FullName }

          gh release view $tag *> $null
          if ($LASTEXITCODE -eq 0) {
            gh release upload $tag $assets --clobber
          } else {
            gh release create $tag $assets --generate-notes
          }
